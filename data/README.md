# Runtime data

This directory is reserved for ephemeral chatbot audio:

- `audio_in/`: microphone captures or WAV inputs
- `audio_out/`: synthesized responses
- `samples/`: optional manually curated demonstration audio

Generated WAV files are ignored by Git. Keep research datasets and frozen evaluation outputs under `benchmark/`, not here.
