# QA artifacts

`fleurs_fa_ir_generated_qa_candidates_v1.csv` is the frozen, validated QA
benchmark used by the downstream experiment.

The `candidates/` directory contains pre-validation source tables. These are
provenance artifacts only and must not replace or modify the frozen benchmark.
