# Uploaded dataset artifacts

`PCoQA_Test.pk` is preserved exactly as uploaded with the LLM experiments.
Because pickle files can execute code while loading, only open it in a trusted
environment with code that verifies its provenance.
