# QA benchmark notebooks

Store QA generation and human-validation notebooks here. The frozen QA artifact in `qa/` must not be modified by exploratory notebooks.

`01_error_propagation_dataset_creation_v1.ipynb` is the preserved dataset and
QA-preparation notebook that was previously misplaced under `analysis/inputs/`.
